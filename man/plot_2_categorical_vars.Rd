% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_2_categorical_vars.R
\name{plot_2_categorical_vars}
\alias{plot_2_categorical_vars}
\title{Stacked percent bar chart for two categorical variables}
\usage{
plot_2_categorical_vars(
  d,
  xvar,
  yvar,
  xvar_label = NULL,
  yvar_label = NULL,
  yvar_colors = NULL,
  title = NULL,
  title_nchar_wrap = 30,
  show_effect_size = TRUE,
  n_pct_size = 3.5,
  inside_bar_stats = c("pct", "n", "n_and_pct", "none"),
  pct_digits = 0,
  plot_horizontal = FALSE,
  flip = FALSE,
  xaxis_labels_nchar_wrap = 20,
  y_max = 110,
  n_ypos = y_max - 5,
  include_overall_bar = FALSE
)
}
\arguments{
\item{d}{A data.frame containing the variables.}

\item{xvar}{Character scalar, name of the categorical x variable (column in d).}

\item{yvar}{Character scalar, name of the categorical y variable (column in d).}

\item{xvar_label}{Optional character scalar to use for the x axis label.}

\item{yvar_label}{Optional character scalar to use for the legend label.}

\item{yvar_colors}{Optional character vector of colours to use for the y-variable levels.
Must be length equal to \code{nlevels(d[[yvar]])}. Provide colour names (e.g. "red") or hex
codes (e.g. "#FF0000"). If NULL (default) ggplot2's default palette is used.}

\item{title}{Optional character scalar for the plot title.}

\item{title_nchar_wrap}{Integer scalar. Maximum number of characters per line of title.}

\item{show_effect_size}{Logical; if TRUE the subtitle will include Cramer's V and p-value.}

\item{n_pct_size}{Numeric scalar. Point size used for the percent labels inside
the stacked bars and for the group N labels above bars. Must be a single
positive numeric value.}

\item{inside_bar_stats}{Character scalar controlling what statistics are
printed inside the stacked bars. One of \code{"pct"} (default; shows within-group
percentage), \code{"n"} (shows count only), \code{"n_and_pct"} (shows count with
percentage below it), or \code{"none"} (no labels inside bars).}

\item{pct_digits}{Integer scalar (default 0). Number of decimal places to show
for the within-group percent labels (e.g. 0 => "12\%", 1 => "12.3\%"). Must be
a single non-negative numeric value.}

\item{plot_horizontal}{Logical scalar. Deprecated/alias for \code{flip}. If TRUE the plot
will be shown with horizontal bars. Prefer using \code{flip}.}

\item{flip}{Logical scalar (default FALSE). If TRUE the plot is flipped to show
horizontal bars.}

\item{xaxis_labels_nchar_wrap}{Integer scalar. Maximum number of characters
per line for x-axis group labels. Longer labels will be wrapped to multiple lines.}

\item{y_max}{Numeric scalar. Maximum y-axis limit for the plot.}

\item{n_ypos}{Numeric scalar. y-axis position for the group N labels.}

\item{include_overall_bar}{Logical scalar (default FALSE). If TRUE, a pooled
"Overall" bar showing the marginal distribution of \code{yvar} across all
observations is prepended to the left of the per-group bars, separated by a
dashed vertical line.}
}
\value{
A list of the ggplot2 object, Cramer's V effect size, p-value.
}
\description{
Create a stacked percent bar chart of an x categorical variable broken down by a
second categorical variable. The plot includes within-group percentages, group
sample sizes above each bar, and (optionally) a subtitle showing Cramer's V and
the test p-value.
}
\examples{
data(mtcars)
mtcars$cyl <- factor(mtcars$cyl)
mtcars$gear <- factor(mtcars$gear)
p <- plot_2_categorical_vars(
  d = mtcars,
  xvar = "cyl",
  yvar = "gear",
  xvar_label = 'Cylinders',
  yvar_label = 'Gears'
)
p$ggplot

}

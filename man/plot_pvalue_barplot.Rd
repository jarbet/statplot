% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pvalue_barplot.R
\name{plot_pvalue_barplot}
\alias{plot_pvalue_barplot}
\title{Plot p-value barplot}
\usage{
plot_pvalue_barplot(
  data,
  x = "pvalue",
  y = "cell_line",
  fill = NULL,
  alpha = 0.05,
  width = 0.6,
  xlim = NULL,
  xbreaks = NULL,
  xlab = "p-value",
  vline = TRUE,
  vline_linetype = "dashed",
  vline_color = "red",
  show_y_labels = FALSE,
  mlog10_transform_pvalue = FALSE
)
}
\arguments{
\item{data}{A data.frame or tibble containing the variables.}

\item{x}{Character, name of the column with raw p-values (default \"pvalue\").}

\item{y}{Character, name of the column for y-axis categories (default \"cell_line\").}

\item{fill}{Character or NULL, column name to use for fill; if NULL draw solid black bars.}

\item{alpha}{Numeric significance threshold for the vertical line (default 0.05).}

\item{width}{Numeric bar width.}

\item{xlim}{Numeric vector of length 2 giving x-axis limits; computed if NULL.}

\item{xbreaks}{Numeric vector of x-axis breaks; computed if NULL.}

\item{xlab}{Character, x-axis label.}

\item{vline}{Logical, whether to draw a vertical line at alpha (or -log10(alpha)).}

\item{vline_linetype}{Character, linetype for the vertical line.}

\item{vline_color}{Character, color for the vertical line.}

\item{show_y_labels}{Logical, whether to show y-axis labels (default FALSE).}

\item{mlog10_transform_pvalue}{Logical; when TRUE compute -log10(p) for plotting/order.}
}
\value{
A ggplot2 object (invisible when returned from functions).
}
\description{
Create a horizontal barplot of p-values (optionally -log10 transformed) with an
optional significance vertical line and optional fill mapping.
}
\examples{
set.seed(123)
n <- 4
example_df <- tibble::tibble(
  cell_line = paste0("Cell", sprintf("\%02d", 1:n)),
  pvalue = 10^(-runif(n, 0.2, 2.8)),
  group = rep(c("A", "B"), length.out = n)
)
example_df$cell_line <- factor(
  example_df$cell_line,
  levels = rev(example_df$cell_line)
)
plot_pvalue_barplot(
  data = example_df,
  x = "pvalue",
  y = "cell_line",
  fill = NULL,
  mlog10_transform_pvalue = TRUE,
  show_y_labels = TRUE
)
}

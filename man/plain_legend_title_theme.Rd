% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{plain_legend_title_theme}
\alias{plain_legend_title_theme}
\title{Force a plot's legend title to plain text}
\usage{
plain_legend_title_theme(p)
}
\arguments{
\item{p}{A ggplot object.}
}
\value{
\code{p} with its theme resolved to a complete theme using a plain
\code{\link[ggplot2:element_text]{ggplot2::element_text()}} \code{legend.title}.
}
\description{
Internal helper for functions (e.g. \code{plot_dotmap}, \code{plot_pathways}) that
allow a legend title to be supplied as a plotmath \code{expression()}.
\code{\link[ggtext:element_markdown]{ggtext::element_markdown()}} cannot render an \code{expression()} (it would be
coerced to a literal, deparsed character string, e.g.
\verb{"bold(-log[\\"10\\"] ~ \\"pvalue\\")"}, rather than parsed as plotmath); and a
later \code{theme(legend.title = ggplot2::element_text(...))} cannot simply be
added on top of a plot whose (possibly global, via \code{theme_set()}) legend
title element is \code{element_markdown()}, since ggplot2 >= 4.0 errors when
merging theme elements of different classes for the same slot. This
resolves the plot's current theme (its own local overrides plus the
current global default from \code{\link[ggplot2:theme_get]{ggplot2::theme_get()}}) into a single,
complete theme with a plain-text, bold legend title, using
\link[ggplot2:\%+replace\%]{ggplot2::\%+replace\%} to swap the element in directly rather than merging
it, which sidesteps the class-merge restriction entirely.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_confidence_intervals.R
\name{plot_confidence_intervals}
\alias{plot_confidence_intervals}
\title{Dot-and-whisker plot of estimates with confidence intervals}
\usage{
plot_confidence_intervals(
  data,
  effect_size,
  ci_low,
  ci_high,
  id,
  group_col = NULL,
  dodge_width = 0.3,
  color_col = NULL,
  color_values = NULL,
  show_separators = TRUE,
  shape_col = NULL,
  sep_linetype = "solid",
  sep_linewidth = 0.4,
  sep_color = "black",
  vline_xintercept = 0,
  vline_linetype = "dashed",
  vline_color = "black",
  point_shapes = c(21, 24, 22, 25, 23),
  pvalue_col = NULL,
  combine_pvalue_method = c("fisher", "CMC", "MCM", "cauchy", "minp_bonferroni"),
  pvalue_plot_width = 0.3,
  pvalue_plot_margin = c(5.5, 12, 5.5, 0),
  ...
)
}
\arguments{
\item{data}{A data.frame or tibble containing the columns referenced by
\code{effect_size}, \code{ci_low}, \code{ci_high}, and \code{id}.}

\item{effect_size}{String name of the effect size/estimate column.}

\item{ci_low}{String name of the lower confidence interval column.}

\item{ci_high}{String name of the upper confidence interval column.}

\item{id}{String name of the label/row identifier column. Must be a
\strong{factor}; factor levels control the top-to-bottom row ordering (first
level appears at the top).}

\item{group_col}{Optional string name of a \strong{factor} grouping column. When
provided, points are offset by group for visibility. Factor levels
control the ordering and direction of the dodge offset: the first level
is plotted at the top of each row.}

\item{dodge_width}{Numeric. Total vertical spread across groups. Default \code{0.3}.}

\item{color_col}{Optional string name of a column to use for coloring
segments and points. When \code{NULL} (default), no colors are applied. Common
choices are \code{group_col} to distinguish groups or \code{id} to color by
row. Must be a factor or character column.}

\item{color_values}{Optional named character vector specifying custom colors
for the levels in \code{color_col}. Names should match the factor levels and
values should be valid R color names or hex codes (e.g.,
\code{c(g1 = "#FF0000", g2 = "#0000FF")}). If \code{NULL} (default), ggplot2's
default color scale is used. Ignored if \code{color_col} is \code{NULL}.}

\item{show_separators}{Logical. Whether to draw horizontal separator lines
between rows when \code{group_col} is supplied. Default \code{TRUE}.}

\item{shape_col}{Optional string name of a column to use for point shapes.
When \code{NULL} (default), no shape encoding is applied. Can be a factor or
character column; character columns are coerced to factor with sorted level
ordering for stable shape assignment. Can be used independently or in
combination with \code{color_col}.}

\item{sep_linetype}{Line type for row separator lines. Default \code{"solid"}.}

\item{sep_linewidth}{Line width for row separator lines. Default \code{0.4}.}

\item{sep_color}{Color for row separator lines. Default \code{"black"}.}

\item{vline_xintercept}{Numeric. Position of the vertical reference line. Default \code{0}.}

\item{vline_linetype}{Line type for the vertical reference line. Default \code{"dashed"}.}

\item{vline_color}{Color for the vertical reference line. Default \code{"black"}.}

\item{point_shapes}{Integer vector of point shapes to use when
\code{shape_col} is specified. Must have at least as many elements as there are
levels in \code{shape_col}. Defaults to \code{c(21, 24, 22, 25, 23)} (up to 5
groups). See \code{\link[graphics:points]{graphics::points()}} for shape codes.}

\item{pvalue_col}{Optional string name of a column in \code{data} containing
p-values (one per row). When supplied, a \code{\link[=plot_pvalue_barplot]{plot_pvalue_barplot()}} is
appended to the right using patchwork. When \code{group_col} is specified,
p-values are combined across groups for each \code{id} level using the method
specified by \code{combine_pvalue_method}. When \code{group_col} is \code{NULL}, individual
p-values are displayed directly. Requires the \pkg{patchwork} package.}

\item{combine_pvalue_method}{Character; method for combining p-values when
\code{group_col} is supplied. One of: "fisher" (default), "CMC", "MCM",
"cauchy", "minp_bonferroni". See \code{\link[=combine_pvalues]{combine_pvalues()}} for details.
Ignored if \code{group_col} is \code{NULL}.}

\item{pvalue_plot_width}{Relative width of the p-value panel passed to
\code{patchwork::wrap_plots()} \code{widths}. Default \code{0.3}.}

\item{pvalue_plot_margin}{Numeric vector of length 4 giving the p-value
panel plot margin in points: \code{c(top, right, bottom, left)}. Default
\code{c(5.5, 12, 5.5, 0)} which increases right margin so x-axis labels are
not clipped when the panel is appended to the right.}

\item{...}{Additional arguments passed to \code{\link[=plot_pvalue_barplot]{plot_pvalue_barplot()}} when
\code{pvalue_col} is supplied.}
}
\value{
A \code{ggplot2} object, or a \code{patchwork} object when \code{pvalue_col} is
supplied.
}
\description{
Create a horizontal dot-and-whisker plot showing point estimates and
confidence intervals for labeled rows. Optionally offset points and use
different shapes or colors when a grouping column is supplied. Optionally
append a p-value barplot to the right via patchwork.
}
\examples{
df <- data.frame(
  cell_line = factor(c("A", "B", "C", "A", "B", "C"), levels = c('A', 'B', 'C')),
  est       = c(0.2, -0.1, 0.5, 0.35, 0.05, 0.3),
  conf.low  = c(0.0, -0.3, 0.2, 0.10, -0.10, 0.1),
  conf.high = c(0.4, 0.1, 0.8, 0.60, 0.20, 0.5),
  group     = factor(c("g1", "g1", "g1", "g2", "g2", "g2"), levels = c("g1", "g2")),
  pvalue    = c(0.01, 0.4, 0.001, 0.02, 0.3, 0.0001)
)

# Simplest: no grouping, no color
plot_confidence_intervals(
  df[1:3, ],
  effect_size = "est",
  ci_low = "conf.low",
  ci_high = "conf.high",
  id = "cell_line"
)

# With grouping: separate rows with color by default
plot_confidence_intervals(
  df,
  effect_size = "est",
  ci_low = "conf.low",
  ci_high = "conf.high",
  id = "cell_line",
  group_col = "group",
  color_col = "group"
)

# With grouping: different shapes for groups
plot_confidence_intervals(
  df,
  effect_size = "est",
  ci_low = "conf.low",
  ci_high = "conf.high",
  id = "cell_line",
  group_col = "group",
  shape_col = "group",
  show_separators = FALSE
)

# With grouping: color by group with custom colors
plot_confidence_intervals(
  df,
  effect_size = "est",
  ci_low = "conf.low",
  ci_high = "conf.high",
  id = "cell_line",
  group_col = "group",
  color_col = "group",
  color_values = c(g1 = "#E69F00", g2 = "#56B4E9")
)

# Color by label, shapes by group
plot_confidence_intervals(
  df,
  effect_size = "est",
  ci_low = "conf.low",
  ci_high = "conf.high",
  id = "cell_line",
  group_col = "group",
  shape_col = "group",
  color_col = "cell_line"
)

# Grouping with p-value barplot using Fisher's combined p-value
plot_confidence_intervals(
  df,
  effect_size = "est",
  ci_low = "conf.low",
  ci_high = "conf.high",
  id = "cell_line",
  group_col = "group",
  color_col = "group",
  pvalue_col = "pvalue",
  combine_pvalue_method = "fisher",
  mlog10_transform_pvalue = TRUE
)

# No grouping, but with p-value barplot on the right
plot_confidence_intervals(
  df[1:3, ],
  effect_size = "est",
  ci_low = "conf.low",
  ci_high = "conf.high",
  id = "cell_line",
  pvalue_col = "pvalue",
  mlog10_transform_pvalue = TRUE
)
}

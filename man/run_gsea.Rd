% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_gsea.R
\name{run_gsea}
\alias{run_gsea}
\title{Run Gene Set Enrichment Analysis (GSEA)}
\usage{
run_gsea(
  gene_vec,
  term2gene,
  sort = TRUE,
  p_adjust_method = "BH",
  p_cutoff = 1,
  min_gs_size = 10,
  max_gs_size = 500,
  seed = 1,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{gene_vec}{named numeric vector Gene-level statistics (e.g. log2 fold
change, t-statistic) named by gene symbol.  The vector \strong{must be sorted in
decreasing order} before being passed in (or set \code{sort = TRUE}).}

\item{term2gene}{data.frame Two-column data frame mapping gene-set names to
gene symbols.  Column 1 must be the gene-set / term name; column 2 must be
the gene symbol.  Column names are coerced to \code{"term"} and \code{"gene"}
internally.}

\item{sort}{logical(1) If \code{TRUE} (default), \code{gene_vec} is sorted in
decreasing order before being passed to \code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}}.}

\item{p_adjust_method}{character(1) Method passed to
\code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}} for multiple-testing correction (default
\code{"BH"}).}

\item{p_cutoff}{numeric(1) \code{pvalueCutoff} passed to
\code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}} (default \code{1}, i.e. return all gene sets).}

\item{min_gs_size}{integer(1) Minimum gene-set size (default \code{10}).}

\item{max_gs_size}{integer(1) Maximum gene-set size (default \code{500}).}

\item{seed}{integer(1) Random seed set before calling
\code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}} for reproducibility (default \code{1}).}

\item{verbose}{logical(1) Passed to \code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}} (default
\code{FALSE}).}

\item{...}{Additional arguments forwarded to \code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}}.}
}
\value{
A list with three elements:
\describe{
\item{\code{gsea_result}}{A \code{gseaResult} object returned by
\code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}}.}
\item{\code{gene_vec}}{The (possibly sorted) named numeric vector that was
passed to GSEA — use this as the \code{foldChange} argument of
\code{\link[=plot_pathways]{plot_pathways()}}.}
\item{\code{term2gene}}{The gene-set data frame (columns \code{term} and \code{gene})
that was passed to GSEA — use this as the \code{gene_sets} argument of
\code{\link[=plot_pathway_correlation_network]{plot_pathway_correlation_network()}}.}
}
}
\description{
A wrapper around \code{\link[clusterProfiler:GSEA]{clusterProfiler::GSEA()}} that accepts a pre-ranked gene
vector and a gene-set data frame, runs GSEA, and returns the result together
with the inputs needed by downstream plotting functions
(\code{\link[=plot_pathways]{plot_pathways()}} and \code{\link[=plot_pathway_correlation_network]{plot_pathway_correlation_network()}}).
}
\examples{
\dontrun{
# hallmark_t2g is bundled with the package (columns: term, gene)
data(hallmark_t2g)
set.seed(1)
all_genes <- unique(hallmark_t2g$gene)
gene_vec  <- setNames(rnorm(length(all_genes)), all_genes)

res <- run_gsea(gene_vec, term2gene = hallmark_t2g)
head(as.data.frame(res$gsea_result))
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{table_basic}
\alias{table_basic}
\title{Create a basic table using tinytable}
\usage{
table_basic(
  d,
  digits = NULL,
  replace_missing = "",
  commas_large_numbers = TRUE,
  use_column_labels = TRUE
)
}
\arguments{
\item{d}{data.frame. Dataset to display as a table.}

\item{digits}{numeric or list. Rounding specification for columns:
\itemize{
\item If a single numeric value, applied to all numeric columns.
\item If a numeric vector, one value per column in order. Non-numeric columns
ignore their corresponding value.
\item If a list, a named list mapping column names to number of digits (e.g.,
\code{list(age = 0, salary = 2)}). Unnamed or missing columns keep their
original precision.
}}

\item{replace_missing}{character(1). String to replace missing values with.
Default is empty string ("").}

\item{commas_large_numbers}{logical(1). If TRUE (default), format numeric values
with commas for thousands (e.g., 1,234.56). If FALSE, no comma formatting.}

\item{use_column_labels}{logical(1). If TRUE, use the
\code{label} attribute of columns if available, otherwise use column names.
Passed to \code{tinytable::tt(colnames = ...)}.}
}
\value{
A tinytable object
}
\description{
Wrapper around tinytable functions to create a simple, clean table with
a bold header row and optional column-level rounding control.
}
\examples{
df <- data.frame(
  name = c("Alice", "Bob", NA),
  age = c(25.5, 30.2, 35.8),
  salary = c(50000.123, 60000.456, NA)
)

# Round specific columns by name
table_basic(df, digits = list(age = 1, salary = 0))

# Round by column position (ignores non-numeric columns)
table_basic(df, digits = c(0, 1, 2))

# Disable comma formatting for large numbers
table_basic(df, digits = list(age = 1, salary = 0), commas_large_numbers = FALSE)

}
